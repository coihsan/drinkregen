interface SocialShareProps {
    pageId: string
}

const SocialShare = ({pageId} : SocialShareProps) => {
    if(!pageId) return null

    return (
        <div>
            
        </div>
    )
}
export default SocialShare